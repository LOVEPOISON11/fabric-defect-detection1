# Vercel-optimized dependencies
Flask==2.3.3
numpy>=1.24.0
Pillow>=10.0.0
Werkzeug==2.3.7

# Computer vision (lightweight for Vercel)
opencv-python-headless==4.8.1.78

# ML dependencies (optional - may cause deployment issues on Vercel)
# torch>=2.0.0
# torchvision>=0.15.0
# ultralytics>=8.0.0

# Security
Flask-CORS==4.0.0
